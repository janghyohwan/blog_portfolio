export type ContactInfo = {
  email: string;
  phone: string;
  location: string;
  github: string;
};

export type SocialLink = {
  platform: string;
  url: string;
};

export type ContactPageProps = {
  // Add any props if needed
};
